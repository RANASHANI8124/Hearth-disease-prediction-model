# Hearth-disease-prediction-model
Heart Disease Prediction is a machine learning project that predicts heart disease using patient data. It is built with Python, Pandas, NumPy, and Scikit-learn. A Random Forest model is trained on features like age, blood pressure, cholesterol, and heart rate. It is tested using accuracy on train and test data and can predict early detection. 
